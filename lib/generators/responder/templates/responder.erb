class <%= class_name %>Responder < ApplicationResponder
    topic :<%= topic %>
    def respond(event)
        if event.kind_of? KafkaEvent
            respond_to :<%= topic %>, event.data.to_json, headers: event.headers
        else
            respond_to :<%= topic %>, event.to_json
        end
    end
end