# frozen_string_literal: true

# A generic karafka responder
class <%= class_name %> < ApplicationConsumer
  def consume
    params_batch.each do |message|
      # TO DO - Que debe hacer este consumer?
    end
  end
end
